using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// not implemented

/// <summary>
/// Code Description:
/// Handles errors in game and displays proper error messages.
/// errors are input from other scripts with error ID and Section ID.
/// script has a table for error txt components, multiple section tables to identify errors.
/// </summary>

public class ErrorHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
